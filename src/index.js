import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import ParentApp from './components/parentApp.js'

ReactDOM.render(
  <React.StrictMode>
    <ParentApp />
  </React.StrictMode>,
  document.getElementById('root')
);

